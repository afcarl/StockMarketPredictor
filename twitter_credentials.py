credentials = {
                'ck':'',   # Consumer key
                'cs':'',   # Consumer secret
                'at':'',   # access token
                'ats':''   # access token secret
}

if __name__ == '__main__':
    for key in credentials:
        print (key,credentials[key])
